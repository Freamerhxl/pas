var f:array[0..51,0..51,0..51]of longint;
    a:array[0..51,0..51]of longint;
    i,j,n,m,x1,y1,x2,y2:longint;
begin
  readln(n,m);
  for i:=1 to n do
  for j:=1 to m do
    read(a[i,j]);
  for x1:=1 to n do
  for y1:=1 to m do
  for x2:=1 to n do
end;